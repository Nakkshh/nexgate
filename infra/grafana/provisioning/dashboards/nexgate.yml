apiVersion: 1

providers:
  - name: "NexGate"
    orgId: 1
    folder: "NexGate"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
